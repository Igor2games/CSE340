<section class="account-view" aria-labelledby="account-management-title">
	<h1 id="account-management-title"><%= title %></h1>

	<%- messages() %>

	<% if (errors) { %>
		<ul class="notice">
			<% errors.array().forEach(error => { %>
				<li><%= error.msg %></li>
			<% }) %>
		</ul>
	<% } %>

	<p>You're logged in</p>
</section>
